# All environment variables will be sourced
# and made available.
# Do NOT commit this file to source control

# GitHub configuration. Needed for commenting and for Decap CMS image preview.
GITHUB_REPO_OWNER=your-github-username
GITHUB_REPO_NAME=your-repo-name
# Recommended: set GITHUB_TOKEN to avoid API rate limits (60/hr without, 5000/hr with).
# Required for private repos. Use a PAT with repo (or contents: read) scope.
# GITHUB_TOKEN=your-github-token
# Optional: branch for CMS image proxy (default: main)
# GITHUB_BRANCH=main
